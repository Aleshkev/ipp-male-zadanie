
#include "io.c"

// char peekchar();
// size_t eat_unsigned(size_t min, size_t max, bool *malformed, bool *error);
// void eat_spaces(size_t min, bool *malformed, bool *error);
// void eat_symbol(char s, bool *malformed, bool *error);

// size_t *eat_line_1(size_t *n_dimensions, bool *malformed, bool *error);
// size_t *eat_line_2_or_3(size_t n_dimensions, const size_t *dimensions,
//                         size_t volume, bool *malformed, bool *error);
// unsigned *eat_line_4(size_t n_dimensions, const size_t *dimensions,
//                      size_t volume, bool *malformed, bool *error);